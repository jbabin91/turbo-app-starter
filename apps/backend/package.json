{
  "name": "backend",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "lint": "eslint .",
    "preview": "NODE_ENV=production tsx watch src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.12.0",
    "@repo/api": "workspace:*",
    "hono": "^4.5.3"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "tsx": "^4.16.3"
  }
}
