{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "NODE_ENV=production bun build --minify src/index.ts --target bun --outdir dist",
    "clean": "rm -rf .turbo dist node_modules",
    "dev": "NODE_ENV=development bun run --hot src/index.ts",
    "lint": "eslint .",
    "preview": "NODE_ENV=production bun run dist/index.js",
    "start": "bun run dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@repo/api": "workspace:*",
    "@repo/core": "workspace:*",
    "hono": "^4.5.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "latest"
  }
}
